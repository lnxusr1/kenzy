# Environment / secrets
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
*.egg
pip-wheel-metadata/

# Type checker / linter caches
.mypy_cache/
.ruff_cache/
.pytype/

# Test / coverage
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Downloaded ML models
./models/

# Runtime data
/data/speakers/
/data/home_assistant/

# Operator/site-specific config — never commit (it holds your rooms, HA URL, location,
# auth, etc.). Generic templates live in examples/. (.env is ignored above.)
# Leading slash anchors these to the repo root so they only match the top-level
# operator tree — NOT the packaged defaults under src/kenzy/data/configs/, which
# ship as package data and must stay tracked.
/configs/hosts/
/configs/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/
CLAUDE.md
/models/wakeword
/models/speaker
/design/
/site/
/www/
screenshot_site.py
synth_phrase.py

build_pypi.sh
publish.sh
dev_stack.py
# Voice test rig (dev-box specific: BY Y02 speaker/mic beside the node)
voice_probe.py

# Dev-box household curation (John's real HA layer — not fixture data)
/data/home_assistant/curation.yaml
