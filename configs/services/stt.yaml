# Central, dashboard-managed STT service config (was configs/hosts/office/stt.yaml).
# Deep-merged over the packaged default; seeded to the server by kenzy-deploy
# (seed-don't-clobber) and editable from the dashboard Services tab.
host: "127.0.0.1"
port: 8767
log_level: "info"

whisper:
  model: "small"
  device: "cuda"
  compute_type: "int8"
  language: "en"
